/** How long a typing indicator is considered "active" before expiring (ms) */
export const TYPING_TIMEOUT_MS = 3000;

/** Max age of lastSeen to consider user online (ms) */
export const PRESENCE_ONLINE_THRESHOLD_MS = 60000;

/** Interval for heartbeat updates when user is active (ms) */
export const PRESENCE_UPDATE_INTERVAL_MS = 20000;
